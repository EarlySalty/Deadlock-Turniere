[package]
name = "turnier-db"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[features]
default = []
testing = ["dl-central-db/testing"]

# Persistenz-Fundament: zentraler PgPool-Aufbau und Fehler-Typ. KEINE
# Geschäftslogik — domänenspezifische Queries leben in den Domain-Crates und
# nutzen den hier bereitgestellten Pool.
[dependencies]
turnier-core = { path = "../turnier-core" }
turnier-config = { path = "../turnier-config" }
dl-central-db.workspace = true
sqlx.workspace = true
tokio.workspace = true
thiserror.workspace = true
tracing.workspace = true

[lints]
workspace = true
