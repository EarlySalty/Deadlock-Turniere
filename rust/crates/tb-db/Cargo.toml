[package]
name = "tb-db"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# Persistenz-Fundament: SqlitePool-Aufbau, PRAGMA-Setup, eingebettete Migration,
# Fehler-Typ. KEINE Geschäftslogik — domänenspezifische Queries leben in den
# Domain-Crates und nutzen den hier bereitgestellten Pool.
[dependencies]
tb-core = { path = "../tb-core" }
sqlx.workspace = true
tokio.workspace = true
thiserror.workspace = true
tracing.workspace = true

[lints]
workspace = true
