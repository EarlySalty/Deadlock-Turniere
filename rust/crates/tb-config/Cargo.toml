[package]
name = "tb-config"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# Geschichtete Konfiguration: löst Werte aus Secret-Dateien, Credentials-
# Verzeichnissen und Umgebungsvariablen auf (in dieser Priorität), sonst Default.
[dependencies]
tracing.workspace = true

[lints]
workspace = true
